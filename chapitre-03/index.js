// Création d'une constante (pas de réaffectation possible)
const name = "Amin";

// Uncaught TypeError: invalid assignment to const 'name'
// name = "Yves";

// Création d'une constante (pas de réaffectation possible)
const otherName = "Yves";

console.log(name);
console.log(otherName);
