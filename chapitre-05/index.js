// Une chaîne de caractères avec des guillemets
const firstName = "Amin";

// Une chaîne de caractères avec des apostrophes
const lastName = 'NAIRI';

// Une chaîne de caractère avec un apostrophe
const text = "L'apostrophe s'affiche ici";

// Un caractère d'echappement (\')
const paragraph = 'Le " s\'affiche ici';

const first = "Amin";
const last = "NAIRI";

// "AminNAIRI"
const fullName = first + last;

// Interpollation de chaîne de caractères "Amin NAIRI"
const name = `${firstName} ${lastName}`;

// Récupère la taille d'une chaîne de caractères
const length = text.length;
