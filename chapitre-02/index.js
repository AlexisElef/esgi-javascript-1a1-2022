// Déclarer et stocker une variable
let name = "Amin";

console.log(name);
// Amin

name = "Yves";

console.log(name);
// Yves

// Une variable qui commence par une majuscule
const Text = "Hello";

// Une variable qui commence par un underscore
const _private = true;

// Une variable qui commence par un dollar
const $observable = "texte";

// Une variable avec un nom composé (peu recommandé)
const speed_of_light = 3.14;

// Une variable avec un nom composé (recommandé, camelCase)
const speedOfLight = 3.14;
