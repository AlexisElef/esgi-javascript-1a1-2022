// Entier (number)
const vatRate = 20;

// Flottant (number)
const vatRateDecimal = 0.2;

// Notation exponentielle
const powerOfTen = 10e5;

// Affiche 1000000
console.log(powerOfTen);

// Décris un nombre qui n'a aucune limite
const positiveInfinity = Infinity;

// Afficheg Inifnity
console.log(positiveInfinity);

// Décris un nombre qui n'a aucune limite (négative)
const negativeInfinity = -Infinity;

// Affiche -Infinity
console.log(negativeInfinity);

// Représente une erreur de calcul
const notANumber = NaN;

// Affiche NaN
console.log(notANumber);
